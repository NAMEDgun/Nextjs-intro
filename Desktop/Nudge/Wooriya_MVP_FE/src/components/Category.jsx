import React from "react";
import styles from "@/app/styles/cagtegory.module.css";

const Category = ({
    sports,
    med,
    beauty,
    art,
    life,
    pet,
    food,
    etc,
}) => {
    return (
        <div className={styles.container} onClick={onClick}>
            
        </div>
    )
}
